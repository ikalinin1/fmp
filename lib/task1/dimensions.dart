import 'package:flutter/cupertino.dart';

abstract class Dimensions {
  static const double rowHeight = 50;
  static const double cardWidth = 75;
  static const EdgeInsets defaultInsets = EdgeInsets.all(10);
}
